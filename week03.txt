3주차 수업내용
- linux 명령어 기초 복습 (ls touch cat)
  cd .. (현재 폴더에서 상위 폴더로 이동)
- git 명령어 기초 복습 (init add commit log status push 등)
- git clone "원격리포지토리 주소" .
- git config --list
- git remote remove origin (기존의 원격리포지토리 주소를 변경하려면 삭제를 해야 한다)
- git remote add origin "연결할 원격리포지토리 주소"